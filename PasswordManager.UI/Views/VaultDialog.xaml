<Window x:Class="PasswordManager.UI.Views.VaultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Vault" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    
    <Border Style="{StaticResource Card}" Margin="20">
        <StackPanel>
            <TextBlock Text="Create New Vault" 
                      FontSize="20" 
                      FontWeight="Bold" 
                      Margin="0,0,0,20"/>

            <TextBlock Text="Vault Name" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBox x:Name="NameTextBox" 
                    Style="{StaticResource ModernTextBox}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBox x:Name="DescriptionTextBox" 
                    Style="{StaticResource ModernTextBox}"
                    AcceptsReturn="True"
                    Height="80"
                    TextWrapping="Wrap"
                    Margin="0,0,0,15"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                       Content="Cancel" 
                       Style="{StaticResource SecondaryButton}"
                       Click="CancelButton_Click"/>
                <Button Grid.Column="2" 
                       Content="Create Vault" 
                       Style="{StaticResource PrimaryButton}"
                       Click="CreateButton_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
